# Vendored Web Assets

Populate this directory for fully offline browser runtime support:

```bash
./scripts/vendor_web_assets.sh
```

This downloads local copies of Pyodide and CodeMirror assets used by `index.html`.
